[pytest]
# discovery: we include both test directories
testpaths = system_tests regression_test_nightly

# we register the markers used by CI
markers =
    smoke: Quick smoke tests (fast, run on PRs)
    regression: Full end-to-end regression (slow, nightly/release)
    slow: Very slow tests